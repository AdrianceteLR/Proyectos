<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TareasApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrincipal</item>
        <!-- Definir el estilo para el menú desplegable -->
        <item name="popupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="Theme.TareasApp" parent="Base.Theme.TareasApp" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Ajustes del menú desplegable -->
        <!-- Por ejemplo, puedes personalizar el color de fondo -->
        <item name="android:background">@color/colorPrincipal</item>
    </style>
</resources>